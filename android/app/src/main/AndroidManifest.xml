<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Sin permiso de galería: la foto de perfil se elige con el Photo Picker
         del sistema (react-native-image-picker usa PickVisualMedia de
         androidx.activity, que no requiere READ_MEDIA_IMAGES ni
         READ_EXTERNAL_STORAGE). Declararlos sin usarlos disparaba la política
         de Google Play "Use alternative system pickers for photos / videos". -->

    <!-- react-native-fs declara WRITE_EXTERNAL_STORAGE en su manifest (y Android
         infiere READ_EXTERNAL_STORAGE a partir de él), pero en esta app RNFS solo
         escribe en el caché privado (CachesDirectoryPath), que nunca requiere este
         permiso. Se elimina explícitamente del merge para no disparar la misma
         política de Google Play. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     tools:node="remove"/>

    <!-- Declarar CAMERA hace que Android infiera hardware.camera como REQUERIDO,
         lo que oculta la app en Play para dispositivos sin cámara (tablets, etc.).
         La cámara es opcional (la foto de perfil también puede venir de la galería),
         así que marcamos las features como no obligatorias. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- La app no usa publicidad; se elimina AD_ID para que ninguna
         dependencia transitiva (Firebase, GMS) lo inyecte. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
                     tools:node="remove"/>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true"
        android:theme="@style/SplashTheme">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
